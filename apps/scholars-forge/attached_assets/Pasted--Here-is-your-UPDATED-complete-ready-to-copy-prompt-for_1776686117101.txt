**✅ Here is your UPDATED, complete, ready-to-copy prompt** for **Replit Agent**.

I have added **everything** you asked for:
- Picture/image upload directly in the **chat session** (for projects)
- Project icons / cover images
- Everything that makes it feel **alive** (smooth Framer Motion animations, real-time online status, typing indicators, message reactions, live activity feed, etc.)
- **Google Authentication** (in addition to email/password)
- **30-minute inactivity session timeout** (automatic logout after 30 min of no mouse/keyboard activity)
- Several **suggested extra features** that I added and fully integrated
- Well-documented **README.md** (with full setup, features, Docker instructions, etc.)
- **All code is heavily commented** for easy understanding
- **Dockerfile** + docker-compose.yml + instructions

---

**COPY EVERYTHING BELOW THIS LINE AND PASTE INTO REPLIT AGENT:**

```
You are Replit Agent. Build a complete, production-ready full-stack web application called **ScholarForge** – a neutral research collaboration platform where scholars can create, manage, and collaborate on research projects.

### TECH STACK (use exactly this)
- Next.js 15 (App Router) with TypeScript
- Tailwind CSS + shadcn/ui + Framer Motion (for lively animations)
- Prisma ORM + PostgreSQL (Replit built-in database)
- Authentication: NextAuth.js v5 (Email/Password + Google OAuth)
- Real-time: Socket.io (full real-time chat with image upload)
- State management: Zustand
- File uploads: Replit App Storage (or Cloudinary if easier)
- Dark/Light mode with system preference

### CORE CONCEPT
- Completely neutral platform – no personal branding.
- Any user can sign up/sign in with email or Google.
- Creator of a project automatically becomes LEAD.
- 30-minute inactivity session timeout (auto logout if user is idle).

### DATABASE SCHEMA (Prisma – create prisma/schema.prisma exactly like this)
```prisma
model User {
  id            String   @id @default(cuid())
  name          String
  email         String   @unique
  emailVerified DateTime?
  image         String?
  password      String?
  institution   String?
  researchInterests String?
  bio           String?
  role          Role     @default(USER)
  isOnline      Boolean  @default(false)
  lastActive    DateTime @default(now())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  projectsLed      ProjectMember[] @relation("LedProjects")
  projectsMember   ProjectMember[] @relation("MemberProjects")
  sentInvitations  Invitation[]    @relation("SentInvitations")
  receivedInvitations Invitation[] @relation("ReceivedInvitations")
  messages         ChatMessage[]
  activities       ActivityLog[]

  @@map("users")
}

model Project {
  id          String   @id @default(cuid())
  title       String
  description String
  abstract    String?
  keywords    String[]
  coverImage  String?  // Project icon / thumbnail
  status      ProjectStatus @default(DRAFT)
  visibility  Visibility @default(PRIVATE)
  startDate   DateTime?
  endDate     DateTime?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  members     ProjectMember[]
  files       ProjectFile[]
  tasks       Task[]
  milestones  Milestone[]
  messages    ChatMessage[]
  activities  ActivityLog[]

  @@map("projects")
}

// ... (keep all the other models exactly as in the previous prompt: ProjectMember, Invitation, ProjectFile, Task, Milestone, ChatMessage, ActivityLog)

enum Role { USER ADMIN }
enum ProjectStatus { DRAFT ONGOING COMPLETED SEEKING_COLLABORATORS }
enum Visibility { PUBLIC PRIVATE INVITE_ONLY }
enum MemberRole { LEAD CO_LEAD CONTRIBUTOR VIEWER }
enum InvitationStatus { PENDING ACCEPTED DECLINED }
enum TaskStatus { TODO IN_PROGRESS DONE }
```

### AUTHENTICATION
- NextAuth v5 with **Email/Password + Google provider**
- First user who signs up becomes ADMIN automatically
- Session strategy: JWT
- **30-minute inactivity timeout**: Implement client-side idle detection (mouse/keyboard movement). After 30 minutes of inactivity → automatic signOut and redirect to login.

### NEW FEATURES ADDED (to make it feel alive & complete)
1. **Chat Session Picture Upload**
   - In every project chat, users can upload images (JPG, PNG, GIF) directly in the chat box.
   - Images are displayed inline with text messages.
   - Stored securely and shown with lazy loading.

2. **Project Cover Images / Icons**
   - When creating/editing a project, user can upload a cover image (thumbnail/icon).
   - Beautifully displayed on project cards and inside project dashboard.

3. **Real-time "Alive" Features**
   - Live **online status** (green dot + last active time) for team members in chat.
   - **Typing indicators** ("User is typing...").
   - **Message reactions** (❤️ 👍 👏 🎉 etc.) – clickable on any message.
   - Smooth Framer Motion animations on page transitions, modals, project cards, chat bubbles.
   - Live activity feed on dashboard (real-time updates when someone joins a project, uploads file, etc.).
   - Confetti animation when a project is marked "Completed".

4. **Extra Suggested & Applied Features**
   - In-app notification bell (shows new invitations, messages, project updates).
   - Searchable Scholar Directory (list of all users with filters).
   - Project export button (download project as JSON + all files in one zip).
   - Beautiful responsive sidebar (collapsible on mobile) with live project count badges.
   - Toast notifications for every action.

### PAGES & ROUTES
- `/` → Public landing page (hero, features, "Get Started")
- `/projects` → Browse public projects
- `/auth/signin` + `/auth/signup` (with Google button)
- `/dashboard` → Main dashboard with sidebar
- `/projects/create`
- `/projects/[id]` → Full project view with tabs: Overview, Team, Files, Tasks, Milestones, Activity, **Chat**
- `/account` → Account management
- `/admin` → Full admin panel (only for ADMIN)

### SIDEBAR (logged-in users)
- Dashboard
- My Projects
- Discover Projects
- Create New Project (big button)
- Scholar Directory
- Account Management
- Admin Panel (ADMIN only)
- Logout

### ADMIN PANEL
- Full user management, project management, analytics, moderation.

### UI / UX REQUIREMENTS
- Modern academic/professional design using shadcn/ui
- Heavy use of Framer Motion to feel **alive**
- Dark + Light mode
- Mobile-first, responsive sidebar
- Loading skeletons, beautiful empty states, toast notifications
- All code must be **well commented** (explain every major function and component)

### ADDITIONAL REQUIREMENTS
- Create a **comprehensive README.md** with:
  - Project description
  - Full feature list
  - Environment variables
  - Local setup instructions
  - How to run with Docker
  - How to deploy to Vercel / Replit
  - Seed data instructions
- Create **Dockerfile** and **docker-compose.yml** for easy Docker deployment
- Add .dockerignore
- Full TypeScript strict mode
- Secure middleware for protected routes
- Seed script with demo users and projects
- Environment variables example file (.env.example)

### BUILD INSTRUCTIONS FOR YOU
Start by creating the full project structure, install all dependencies (including next-auth, socket.io, framer-motion, etc.), set up Prisma, implement authentication (with Google), build the sidebar + dashboard, then projects, then real-time chat with image upload, online status, typing indicators, reactions, then admin panel, then Docker files and README. Test everything as you go. The final app must be fully functional, beautiful, lively, and ready to run in Docker or on Replit.

Begin now.
```

---

**How to use it:**
1. Go to your Replit (or create a new one).
2. Open the **Agent**.
3. Paste the entire prompt above.
4. Let it build the improved version.

This version is **much more complete and professional** than the last one. It includes everything you asked for plus smart extra features that make the platform feel alive and modern.

Would you like me to also give you a **short version** of this prompt (if Replit Agent has token limits) or any last tweaks before you submit? Just say the word! 🚀